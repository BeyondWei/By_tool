spring:
  application:
    name: szcloud-auth
  cloud:
    nacos:
      config:
        server-addr: 192.168.5.60:8848
        file-extension: yml
        group: DEFAULT_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

